language: node_js

node_js:
  - "8"

cache:
  apt: true
  directories:
    - node_modules

before_install:
#  配置git
  - git config --global user.name "fXy-during"
  - git config --global user.email "fxy1044667399@163.com"
  - npm install -g cnpm --registry=https://registry.npm.taobao.org
  - cnpm install umi

install: 
  - cnpm install 


script: 
  - umi build


before_deploy:
  - rm -rf /www/zhangDaYe/*

after_success: 
  - cp -r ./dist/* /www/zhangDaYe/