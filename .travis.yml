language: node_js

node_js:
  - "8"

# os: windows

cache:
  apt: true
  directories:
    - node_modules

addons:
    ssh_known_hosts: 
      120.79.188.124

before_install:
#  配置git
  - git config --global user.name "fXy-during"
  - git config --global user.email "fxy1044667399@163.com"
  - npm install -g cnpm --registry=https://registry.npm.taobao.org
  - cnpm install umi

install: 
  - cnpm install 


script: 
  # - umi build
  - true

# before_deploy:
#   - rm -rf /www/zhangDaYe/*

after_success: 
    - git add .
    - git commit -m "feat: travis commit"
    - git push origin master 